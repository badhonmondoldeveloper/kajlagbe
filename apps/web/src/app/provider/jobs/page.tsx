import { RoutePlaceholder } from "@/components/placeholders/route-placeholder";

export default function AvailableCustomerJobsPage() {
  return (
    <RoutePlaceholder
      title="Available Customer Jobs"
      category="Provider Portal"
      moduleTarget="Module 05"
      description="Browse nearby customer requests and submit quotation bids."
    />
  );
}
