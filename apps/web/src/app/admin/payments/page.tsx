import { RoutePlaceholder } from "@/components/placeholders/route-placeholder";

export default function PaymentGatewaysLedgerPage() {
  return (
    <RoutePlaceholder
      title="Payment Gateways & Ledger"
      category="Admin Control"
      moduleTarget="Module 10"
      description="Reconcile payment transactions across bKash, Nagad, and cards."
    />
  );
}
