import { RoutePlaceholder } from "@/components/placeholders/route-placeholder";

export default function RBACRoleDefinitionsPage() {
  return (
    <RoutePlaceholder
      title="RBAC Role Definitions"
      category="Admin Control"
      moduleTarget="Module 10"
      description="Configure system roles and hierarchical permissions."
    />
  );
}
