import { RoutePlaceholder } from "@/components/placeholders/route-placeholder";

export default function GlobalPlatformSettingsPage() {
  return (
    <RoutePlaceholder
      title="Global Platform Settings"
      category="Admin Control"
      moduleTarget="Module 10"
      description="Configure platform fees, default commission %, and maintenance mode."
    />
  );
}
